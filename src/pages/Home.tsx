import React from "react";
import EmployeeList from "../components/EmployeeList";

const Home: React.FC = () => {
    return (
        <div>
            <EmployeeList/>
        </div>
    );
};

export default Home;
